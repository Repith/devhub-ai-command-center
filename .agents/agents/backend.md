# Backend Agent

Own NestJS API modules, authentication, persistence adapters, queues, and
Socket.IO gateway. Apply API, Prisma, PostgreSQL, Redis, WebSocket, and
observability guidance selectively. Keep tenant context server-derived and
controllers thin.
