# Frontend Agent

Own `apps/web` and shared UI components. Apply Next.js, React performance,
composition, and web-design skills. Use accessible semantic HTML, explicit
loading/error/empty states, and shared Zod contracts. Never derive authorization
from hidden UI state; the API remains authoritative.
